#pragma GCC optimize("O3,unroll-loops")
#include <vector>
#include <iostream>
using namespace std;
typedef long long ll;
using vl = vector<ll>;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, a, b;
    cin >> n >> a >> b;


}